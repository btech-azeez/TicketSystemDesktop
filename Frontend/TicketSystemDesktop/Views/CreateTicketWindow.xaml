<Window x:Class="TicketSystemDesktop.Views.CreateTicketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Support Request" Height="550" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        ResizeMode="NoResize">
    <Border Grid.Row="0" Background="White" CornerRadius="15" Margin="30" Padding="30">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.05"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Create New Ticket" 
                      FontSize="22" FontWeight="ExtraBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,5"/>
            <TextBlock Grid.Row="1" Text="Fill in the details below to request support." 
                      FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,30"/>

            <StackPanel Grid.Row="2">
                <Label Content="SUBJECT" />
                <TextBox x:Name="txtSubject" />
                
                <Label Content="PRIORITY" />
                <ComboBox x:Name="cboPriority" Margin="0,5,0,15">
                    <ComboBoxItem Content="Low"/>
                    <ComboBoxItem Content="Medium" IsSelected="True"/>
                    <ComboBoxItem Content="High"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Row="3">
                <Label Content="DESCRIPTION" />
                <TextBox x:Name="txtDescription" TextWrapping="Wrap" 
                        AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Height="120"/>
            </StackPanel>

            <TextBlock Grid.Row="4" x:Name="lblMessage" Foreground="#EF4444" 
                      TextAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap" FontSize="13"/>

            <DockPanel Grid.Row="5" LastChildFill="False" Margin="0,20,0,0">
                <Button x:Name="btnCancel" Content="Cancel" Width="100" 
                       Click="BtnCancel_Click" Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}" DockPanel.Dock="Left"/>
                <Button x:Name="btnSubmit" Content="Submit Request" Width="180" 
                       Click="BtnSubmit_Click" Foreground="White" Background="{StaticResource PrimaryBrush}" DockPanel.Dock="Right"/>
            </DockPanel>
        </Grid>
    </Border>
</Window>
